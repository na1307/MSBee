<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProductVersion>8.0.50727</ProductVersion>
    <SchemaVersion>2.0</SchemaVersion>
    <ProjectGuid>{77BC76C3-336B-462E-B01F-A105ED16F3F7}</ProjectGuid>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>Microsoft.Build.Extras.FX1_1</RootNamespace>
    <AssemblyName>MSBee</AssemblyName>
    <SccProjectName>SAK</SccProjectName>
    <SccLocalPath>SAK</SccLocalPath>
    <SccAuxPath>SAK</SccAuxPath>
    <SccProvider>SAK</SccProvider>
    <SignAssembly>false</SignAssembly>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
    <RunCodeAnalysis>false</RunCodeAnalysis>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
    <RunCodeAnalysis>false</RunCodeAnalysis>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'FxCop-Release|AnyCPU' ">
    <OutputPath>bin\FxCop-Release\</OutputPath>
    <DefineConstants>CODE_ANALYSIS;TRACE</DefineConstants>
    <Optimize>true</Optimize>
    <DebugType>pdbonly</DebugType>
    <PlatformTarget>AnyCPU</PlatformTarget>
    <RunFxCop>true</RunFxCop>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug-Test|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <OutputPath>bin\Debug-Test\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <DebugType>full</DebugType>
    <PlatformTarget>AnyCPU</PlatformTarget>
  </PropertyGroup>
  <ItemGroup>
    <Reference Include="Microsoft.Build.Framework" />
    <Reference Include="Microsoft.Build.Utilities" />
    <Reference Include="System" />
    <Reference Include="System.Data" />
    <Reference Include="System.Xml" />
  </ItemGroup>
  <ItemGroup>
    <Compile Include="GetFrameworkPath.cs">
      <SubType>Code</SubType>
    </Compile>
    <Compile Include="GetFrameworkSDKPath.cs">
      <SubType>Code</SubType>
    </Compile>
    <Compile Include="GetLocalMachineRegistryValues.cs">
      <SubType>Code</SubType>
    </Compile>
    <Compile Include="GetVisualStudioInteropSearchPaths.cs">
      <SubType>Code</SubType>
    </Compile>
    <Compile Include="GetVisualStudioSearchPaths.cs">
      <SubType>Code</SubType>
    </Compile>
    <Compile Include="GlobalSuppressions.cs" />
    <Compile Include="GenerateResource.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />
    <Compile Include="ResolveComReference.cs" />
    <Compile Include="Strings.Designer.cs">
      <AutoGen>True</AutoGen>
      <DesignTime>True</DesignTime>
      <DependentUpon>Strings.resx</DependentUpon>
    </Compile>
  </ItemGroup>
  <ItemGroup>
    <EmbeddedResource Include="Strings.resx">
      <SubType>Designer</SubType>
      <Generator>ResXFileCodeGenerator</Generator>
      <LastGenOutput>Strings.Designer.cs</LastGenOutput>
    </EmbeddedResource>
  </ItemGroup>
  <ItemGroup>
    <None Include="MSBuildExtras.FX1_1.Common.targets">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </None>
    <None Include="MSBuildExtras.FX1_1.CSharp.targets">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </None>
    <None Include="MSBuildExtras.FX1_1.VisualBasic.targets">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </None>
  </ItemGroup>
  <Import Project="$(MSBuildBinPath)\Microsoft.CSharp.targets" />
  <!-- Import MSBee.settings.targets last so it can overwrite the CoreCompileDependsOn property when necessary. -->
  <Import Project="..\MSBee.Settings.targets" />
  <!-- To modify your build process, add your task inside one of the targets below and uncomment it. 
       Other similar extension points exist, see Microsoft.Common.targets.
  <Target Name="BeforeBuild">
  </Target>
  -->
  <!-- If the FxCop log file exists after a FxCop run, FxCop found violations so produce a build error. -->
  <Target Name="AfterBuild">
    <Error Condition=" '$(RunFxCop)'=='true' and Exists('$(FxCopLogFile)') " Text="FxCop detected build violations." />
  </Target>
  <!-- For FxCop  and Debug builds, don't install MSBee. 
  The Debug builds are setup so that community users can build the project without any tools (including the installer)
  The purpose of FxCop builds is to confirm there are no FxCop violations. -->
  <PropertyGroup Condition=" '$(Configuration)' != 'Debug' And '$(Configuration)' != 'FxCop-Release' ">
    <PostBuildEvent>mkdir "$(ProjectDir)MSBee Installer\MSBee bin"
copy "$(TargetDir)" "$(ProjectDir)MSBee Installer\MSBee bin"
copy "$(ProjectDir)..\doc\MSBee ReadMe.doc" "$(ProjectDir)MSBee Installer\MSBee bin"
copy "$(ProjectDir)..\RCRFX1_1\RCRFX1_1.*" "$(ProjectDir)MSBee Installer\MSBee bin"
call "$(ProjectDir)MSBee Installer\BuildAndRunInstaller.cmd" "$(WiXDir)"</PostBuildEvent>
  </PropertyGroup>
  <PropertyGroup Condition="'$(RunFxCop)'=='true'">
    <PostBuildEvent>del "$(FxCopLogFile)"
call "$(FxCopExtDir)FxCopCmd.exe" /f:"$(TargetPath)" /o:"$(FxCopLogFile)" /console</PostBuildEvent>
  </PropertyGroup>
</Project>
